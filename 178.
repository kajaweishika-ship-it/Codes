#include <stdio.h>
int search(int a[], int n, int x){
    for(int i=0;i<n;i++) if(a[i]==x) return i;
    return -1;
}
int main(){
    int a[]={2,4,6,8,10};
    int pos=search(a,5,8);
    if(pos!=-1) printf("Found at %d",pos);
    else printf("Not found");
}
