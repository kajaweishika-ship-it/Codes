#include <stdio.h>
int main() {
    char s[100]; int i;
    printf("Enter string: ");
    gets(s);
    printf("Digits: ");
    for(i=0; s[i]!='\0'; i++)
        if(s[i]>='0' && s[i]<='9')
            printf("%c", s[i]);
}
