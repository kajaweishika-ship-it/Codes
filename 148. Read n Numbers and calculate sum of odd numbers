#include <stdio.h>
int main() {
    int n, num, sum=0;
    printf("Enter number of elements: ");
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d",&num);
        if(num % 2 != 0) sum += num;
    }
    printf("Sum of odd numbers = %d", sum);
    return 0;
}
