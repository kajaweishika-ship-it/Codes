#include <stdio.h>
int main() {
    char s[50];
    printf("Enter string: ");
    gets(s);
    if(s[0]=='A' || s[0]=='a')
        printf("Starts with A");
    else
        printf("Does not start with A");
}
