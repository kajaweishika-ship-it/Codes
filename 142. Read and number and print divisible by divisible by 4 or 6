#include <stdio.h>
int main() {
    int n, num;
    printf("Enter number of elements: ");
    scanf("%d",&n);
    printf("Numbers divisible by 4 or 6: ");
    for(int i=0;i<n;i++){
        scanf("%d",&num);
        if(num%4==0 || num%6==0) printf("%d ", num);
    }
    return 0;
}
