#include <stdio.h>
#include <string.h>
int main() {
    char s[200];
    int i, count=0;
    printf("Enter sentence: ");
    gets(s);
    for(i=0; s[i]!='\0'; i++)
        if(s[i]=='s' && (s[i+1]==' '||s[i+1]=='\0')) count++;
    printf("Words ending with 's' = %d", count);
}
