#include <stdio.h>
int main() {
    int n;
    printf("Enter number: ");
    scanf("%d", &n);
    switch(n % 5) {
        case 0: printf("Divisible by 5"); break;
        default: printf("Not divisible");
    }
}
