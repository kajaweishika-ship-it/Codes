#include <stdio.h>
#include <string.h>
int main() {
    char s[100], res[100];
    int i, j=0;
    printf("Enter string: ");
    gets(s);
    for(i=0; s[i]!='\0'; i++)
        if(s[i]!=' ') res[j++]=s[i];
    res[j]='\0';
    printf("Without spaces: %s", res);
}
