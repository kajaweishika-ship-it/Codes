#include <stdio.h>
int main() {
    int n, num, min=2147483647;
    printf("Enter number of elements: ");
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d",&num);
        if(num>0 && num<min) min=num;
    }
    printf("Smallest positive number = %d", min);
    return 0;
}
