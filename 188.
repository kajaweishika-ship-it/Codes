#include <stdio.h>
int removeDup(int a[],int n){
    int j=0;
    for(int i=0;i<n-1;i++)
        if(a[i]!=a[i+1]) a[j++]=a[i];
    a[j++]=a[n-1];
    return j;
}
int main(){
    int a[]={1,1,2,2,3,4,4},n=7;
    n=removeDup(a,n);
    for(int i=0;i<n;i++) printf("%d ",a[i]);
}
