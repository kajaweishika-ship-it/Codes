#include <stdio.h>
int main() {
    char ch;
    printf("Enter letter: ");
    scanf("%c",&ch);
    switch(ch >= 'A' && ch <= 'Z') {
        case 1: printf("Uppercase"); break;
        case 0: printf("Lowercase");
    }
}
