#include <stdio.h>
#include <string.h>
int main() {
    char s[200];
    int i, count=1;
    printf("Enter sentence: ");
    gets(s);
    for(i=0; s[i]!='\0'; i++)
        if(s[i]==' ' && s[i+1]!=' ') count++;
    printf("Words = %d", count);
}
