#include <stdio.h>
int main() {
    int n = 122333, freq[10] = {0};
    while(n != 0) {
        freq[n%10]++;
        n /= 10;
    }
    for(int i = 0; i < 10; i++) {
        if(freq[i] != 0)
            printf("%d occurs %d times\n", i, freq[i]);
    }
    return 0;
}
