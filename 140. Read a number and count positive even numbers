#include <stdio.h>
int main() {
    int n, num, count=0;
    printf("Enter number of elements: ");
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d",&num);
        if(num>0 && num%2==0) count++;
    }
    printf("Positive even numbers = %d", count);
    return 0;
}
