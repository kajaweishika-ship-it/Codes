#include <stdio.h>
#include <string.h>
int main() {
    char a[50], b[50];
    printf("Enter first string: ");
    gets(a);
    printf("Enter second string: ");
    gets(b);
    if(stricmp(a,b)==0)
        printf("Equal (case ignored)");
    else
        printf("Not equal");
}
