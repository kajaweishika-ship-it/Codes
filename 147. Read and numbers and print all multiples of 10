#include <stdio.h>
int main() {
    int n, num;
    printf("Enter number of elements: ");
    scanf("%d",&n);
    printf("Multiples of 10: ");
    for(int i=0;i<n;i++){
        scanf("%d",&num);
        if(num % 10 == 0) printf("%d ", num);
    }
    return 0;
}
