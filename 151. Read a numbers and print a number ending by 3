#include <stdio.h>
int main() {
    int n, num, count=0;
    printf("Enter number of elements: ");
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d",&num);
        if(num % 10 == 3) count++;
    }
    printf("Numbers ending with 3 = %d", count);
    return 0;
}
