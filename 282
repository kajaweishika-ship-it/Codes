#include <stdio.h>
int main(){
    int a,b;
    printf("Enter two numbers: ");
    scanf("%d%d",&a,&b);
    switch(b!=0){
        case 1: printf("Remainder=%d",a%b); break;
        case 0: printf("Division by zero not allowed");
    }
}
