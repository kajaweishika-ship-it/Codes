#include <stdio.h>
void findMinMax(int a[], int n){
    int min=a[0],max=a[0];
    for(int i=1;i<n;i++){
        if(a[i]<min) min=a[i];
        if(a[i]>max) max=a[i];
    }
    printf("Min=%d Max=%d",min,max);
}
int main(){
    int a[]={3,8,1,6,9};
    findMinMax(a,5);
}
