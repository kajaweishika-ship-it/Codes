#include <stdio.h>
int main() {
    int n = 5, fact, sum = 0;
    for(int i = 1; i <= n; i++) {
        fact = 1;
        for(int j = 1; j <= i; j++) {
            fact *= j;
        }
        sum += fact;
    }
    printf("Sum of factorial series = %d", sum);
    return 0;
}
