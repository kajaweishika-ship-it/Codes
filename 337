#include <stdio.h>
int main() {
    char s[100]; int i, c=0;
    printf("Enter string: ");
    gets(s);
    for(i=0; s[i]!=' ' && s[i]!='\0'; i++)
        c++;
    printf("Characters before first space = %d", c);
}
