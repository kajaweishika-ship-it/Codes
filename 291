#include <stdio.h>
#include <string.h>
int main() {
    char str[100];
    printf("Enter a string: ");
    gets(str);
    if(strlen(str) == 0)
        printf("Empty string");
    else
        printf("String is not empty");
}
