#include <stdio.h>
#include <string.h>
int main() {
    char s[50];
    int i, found=0;
    printf("Enter string: ");
    gets(s);
    for(i=0; s[i]!='\0'; i++)
        if(s[i]>='0' && s[i]<='9') found=1;
    if(found) printf("Contains digits");
    else printf("No digits");
}
