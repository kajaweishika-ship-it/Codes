#include <stdio.h>
void rotate(int a[],int n){
    int temp=a[0];
    for(int i=0;i<n-1;i++) a[i]=a[i+1];
    a[n-1]=temp;
}
int main(){
    int a[]={1,2,3,4,5};
    rotate(a,5);
    for(int i=0;i<5;i++) printf("%d ",a[i]);
}
