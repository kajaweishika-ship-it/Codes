#include <stdio.h>
int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);

    if (n >= 1 && n <= 100)
        printf("Number is between 1 and 100\n");
    else
        printf("Number is not in the range\n");

    return 0;
}
