#include <stdio.h>
#include <string.h>
int main() {
    char s[50]; int i, len, flag=0;
    printf("Enter string: ");
    gets(s);
    len = strlen(s);
    for(i=0; i<len/2; i++)
        if(s[i]!=s[len-i-1]) flag=1;
    if(flag==0) printf("Palindrome");
    else printf("Not palindrome");
}
