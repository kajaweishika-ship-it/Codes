#include <stdio.h>
#include <string.h>
int main() {
    char s[50];
    printf("Enter email: ");
    gets(s);
    if(strchr(s, '@'))
        printf("Valid Email Format");
    else
        printf("Invalid Email Format");
}
