#include <stdio.h>
int main() {
    int n, sum=0, num;
    printf("How many numbers? ");
    scanf("%d",&n);
    printf("Enter %d numbers: ", n);
    for(int i=0;i<n;i++){
        scanf("%d",&num);
        sum += num;
    }
    printf("Sum = %d", sum);
    return 0;
}
