#include <stdio.h>
#include <string.h>
int main() {
    char s[100];
    int i, freq[256]={0};
    printf("Enter string: ");
    gets(s);
    for(i=0; s[i]!='\0'; i++)
        freq[(int)s[i]]++;
    for(i=0; i<256; i++)
        if(freq[i]!=0)
            printf("%c = %d\n", i, freq[i]);
}
