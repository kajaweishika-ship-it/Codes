#include <stdio.h>
#include <math.h>
int main() {
    int num, sqr;
    printf("Enter a number: ");
    scanf("%d", &num);
    sqr = sqrt(num);
    if (sqr * sqr == num)
        printf("Perfect square\n");
    else
        printf("Not a perfect square\n");
    return 0;
}
