#include <stdio.h>
int main() {
    int a = 12, b = 18, max;
    max = (a > b) ? a : b;
    while(1) {
        if(max % a == 0 && max % b == 0) {
            printf("LCM = %d", max);
            break;
        }
        max++;
    }
    return 0;
}
