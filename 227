#include <stdio.h>
void unique(int a[], int n){
    for(int i=0;i<n;i++){
        int c=0;
        for(int j=0;j<n;j++)
            if(a[i]==a[j]) c++;
        if(c==1) printf("%d ", a[i]);
    }
}
int main(){
    int a[]={1,2,2,3,4,4,5};
    unique(a,7);
}
