#include <stdio.h>
int main() {
    int n = 123456, sum = 0, digit;
    while(n != 0) {
        digit = n % 10;
        if(digit % 2 != 0) sum += digit;
        n /= 10;
    }
    printf("Sum of odd digits = %d", sum);
    return 0;
}
