#include <stdio.h>
int main() {
    char ch;
    printf("Enter a character: ");
    scanf(" %c", &ch);

    if (ch >= 'a' && ch <= 'z')
        printf("Lowercase Letter\n");
    else
        printf("Not Lowercase Letter\n");

    return 0;
}
