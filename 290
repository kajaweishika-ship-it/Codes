#include <stdio.h>
int main(){
    int y;
    printf("Enter year: ");
    scanf("%d",&y);
    switch((y%4==0 && y%100!=0) || y%400==0){
        case 1: printf("Leap Year"); break;
        case 0: printf("Not Leap Year");
    }
}
