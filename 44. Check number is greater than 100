#include <stdio.h>
int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);

    if (n > 100)
        printf("Number is greater than 100\n");
    else
        printf("Number is 100 or less\n");

    return 0;
}
