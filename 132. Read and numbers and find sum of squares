#include <stdio.h>
int main() {
    int n, num, sum=0;
    printf("Enter number of elements: ");
    scanf("%d", &n);
    for(int i=0; i<n; i++){
        scanf("%d", &num);
        sum += num*num;
    }
    printf("Sum of squares = %d", sum);
    return 0;
}
