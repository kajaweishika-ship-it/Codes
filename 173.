#include <stdio.h>
int maxElement(int a[], int n){
    int max=a[0];
    for(int i=1;i<n;i++) if(a[i]>max) max=a[i];
    return max;
}
int main(){
    int a[]={2,8,5,1,9};
    printf("Max = %d", maxElement(a,5));
}
