#include <stdio.h>
int countEven(int a[], int n){
    int c=0;
    for(int i=0;i<n;i++) if(a[i]%2==0) c++;
    return c;
}
int main(){
    int a[]={2,5,8,7,6};
    printf("Even count = %d", countEven(a,5));
}
