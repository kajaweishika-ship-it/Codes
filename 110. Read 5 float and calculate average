#include <stdio.h>
int main() {
    float arr[5], sum=0;
    printf("Enter 5 float numbers: ");
    for(int i=0;i<5;i++){
        scanf("%f",&arr[i]);
        sum += arr[i];
    }
    printf("Average = %.2f", sum/5);
    return 0;
}
